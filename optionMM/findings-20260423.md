Findings

  - High The dispatcher still does synchronous recovery-handle lookups after every submit in /abs/path/D:/workspace/optionMM/src/engine/trading_engine.cpp:1783, /abs/path/D:/workspace/optionMM/src/engine/trading_engine.cpp:1815, and /
    abs/path/D:/workspace/optionMM/src/engine/trading_engine.cpp:1848. Those calls are not O(1): /abs/path/D:/workspace/optionMM/src/gateway/femas_gateway.cpp:470, /abs/path/D:/workspace/optionMM/src/gateway/femas_gateway.cpp:492, /abs/
    path/D:/workspace/optionMM/src/gateway/ctp_gateway.cpp:293, and /abs/path/D:/workspace/optionMM/src/gateway/sim_gateway.cpp:173 all lock and scan state. That work sits directly in the strategy -> gateway stage; the clean fix is to
    return recovery metadata from send_order / send_quote or fill it in later from the ack path.
  - High The production gateway callback path is still mutex + linear-scan heavy. FEMAS lookup helpers in /abs/path/D:/workspace/optionMM/src/gateway/femas_gateway.cpp:121 and /abs/path/D:/workspace/optionMM/src/gateway/
    femas_gateway.cpp:149 scan fixed arrays, and they are used from /abs/path/D:/workspace/optionMM/src/gateway/femas_gateway.cpp:705, /abs/path/D:/workspace/optionMM/src/gateway/femas_gateway.cpp:733, and /abs/path/D:/workspace/
    optionMM/src/gateway/femas_gateway.cpp:815 while holding state_mutex_. CTP and FEMAS also still log every fill on the callback thread in /abs/path/D:/workspace/optionMM/src/gateway/ctp_gateway.cpp:519 and /abs/path/D:/workspace/
    optionMM/src/gateway/femas_gateway.cpp:749. Flat lookup tables keyed by local ID / sys ID / client ID, plus sampled or disabled fill logs, would reduce callback-route latency variance materially.
  - High The pricer hot path still overcomputes per option. It selects the product surface through an IVolSurface* at /abs/path/D:/workspace/optionMM/src/engine/trading_engine.cpp:1282, so every option repricing pays virtual dispatch
    at /abs/path/D:/workspace/optionMM/src/engine/trading_engine.cpp:1324, and the surface implementations then linearly scan expiry slices in /abs/path/D:/workspace/optionMM/include/pricing/svi.h:45, /abs/path/D:/workspace/optionMM/
    include/pricing/wing.h:51, and /abs/path/D:/workspace/optionMM/src/pricing/orc_wing.cpp:204. After that the engine runs three full Black-76 sweeps at /abs/path/D:/workspace/optionMM/src/engine/trading_engine.cpp:1330 and still writes
    full Greeks snapshots at /abs/path/D:/workspace/optionMM/src/engine/trading_engine.cpp:1360 even though the strategy only consumes bid, ask, delta, and vega. A typed per-product pricer with cached expiry buckets and a minimal hot-
    path result would be the next major CPU-side win.
  - High The “precomputed” Black-76 path still reconstructs values it should have eliminated. /abs/path/D:/workspace/optionMM/src/pricing/black76.cpp:58, /abs/path/D:/workspace/optionMM/src/pricing/black76_avx2.cpp:253, and /abs/path/D:/
    workspace/optionMM/src/pricing/black76_avx512.cpp:251 recover r from disc and T just to produce theta. If theta and rho are not needed on the quoting path, stop computing them there; if they are, precompute the exact terms once
    instead of re-deriving them in every batch.
  - Medium The hot tick object is larger than the fast path needs. /abs/path/D:/workspace/optionMM/include/common/types.h:107 makes MarketTick 256 bytes, and that object is copied through the feed ring, tick_snapshot_ in /abs/path/D:/
    workspace/optionMM/include/engine/trading_engine.h:319, and the pricer write at /abs/path/D:/workspace/optionMM/src/engine/trading_engine.cpp:1234, while the strategy later only needs top-of-book data in /abs/path/D:/workspace/
    optionMM/src/strategy/option_mm_core.cpp:478. Splitting the hot path into a compact TOB tick and keeping full depth/open-interest fields off to the side would cut cache traffic and ring-copy cost.
  - Medium The arbitrage sidecar is still poll-driven and continuously republishes monitor state. /abs/path/D:/workspace/optionMM/src/engine/trading_engine.cpp:1531 runs evaluate() every loop and idles with a 100 microsecond sleep at /
    abs/path/D:/workspace/optionMM/src/engine/trading_engine.cpp:1571; each PCP evaluation scans all pairs in /abs/path/D:/workspace/optionMM/src/strategy/pcp_arbitrage.cpp:204 and rebuilds the full pair monitor snapshot in /abs/path/D:/
    workspace/optionMM/src/strategy/pcp_arbitrage.cpp:290. That burns CPU and cache even when no arb opportunity exists. A coalesced “underlying moved” mailbox plus decimated monitor publication would make this much cheaper.
  - Medium The latency harness is still adding simulator-side noise, which makes regression detection weaker than it should be. /abs/path/D:/workspace/optionMM/src/gateway/sim_gateway.cpp:239 forces a 50 microsecond sleep in low-latency
    mode, falls back to 5 milliseconds at /abs/path/D:/workspace/optionMM/src/gateway/sim_gateway.cpp:249, and both order and quote processing serialize on one mutex in /abs/path/D:/workspace/optionMM/src/gateway/sim_gateway.cpp:255
    and /abs/path/D:/workspace/optionMM/src/gateway/sim_gateway.cpp:348. For benchmark fidelity, add a benchmark-only zero-sleep or deadline-wheel simulator path.
  - Medium The release build is still a portable baseline rather than a deployment-tuned build. /abs/path/D:/workspace/optionMM/CMakeLists.txt:77 intentionally avoids host-specific flags, and /abs/path/D:/workspace/optionMM/
    CMakeLists.txt:82 uses only -O3 for release. On a fixed trading host, an opt-in native profile with -march / -mtune, LTO/IPO, and ideally PGO against the latency harness should shave more latency without touching the architecture.

  Assumptions

  - This is a code-inspection review; I did not rerun test_latency in this pass.
  - I prioritized the option_mm_core direct-replace path and production gateway callback/send paths over UI, gRPC throughput, and SQLite bulk persistence speed.

  Priority

  1. Remove send-path recovery lookups and flatten gateway state tables.
  2. Split the pricer into a typed minimal hot path and a colder full-Greeks path.
  3. Shrink the hot tick representation to top-of-book data.
  4. Make arbitrage and the simulator benchmark path event-driven instead of sleep-poll based.